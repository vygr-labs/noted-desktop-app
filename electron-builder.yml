productName: Astro Electron App
artifactName: ${productName}-${os}-${arch}.${ext}
win:
  target: nsis
linux:
  target: AppImage
  category: Utility
mac:
  target: dmg
  category: public.app-category.developer-tools
files: ['src/backend/build/**/*', 'dist/**/*']
extraResources: ['./assets/**']
directories:
  buildResources: 'assets'
